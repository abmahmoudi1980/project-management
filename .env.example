# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-here-at-least-32-characters
JWT_ACCESS_EXPIRY=15m
JWT_REFRESH_EXPIRY=168h

# Email Configuration (Gmail SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password-here

# Application URLs
APP_URL=http://localhost:5173
API_URL=http://localhost:3000

# Database Configuration
# Backend prefers DATABASE_URL (see `backend/config/database.go`).
# Note: current code defaults to password "1" and db "project_management" if unset.
DATABASE_URL=postgres://postgres:1@localhost:5432/project_management?sslmode=disable

# Migration runner (`backend/cmd/migrate`) can also use these values.
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=1
DB_NAME=project_management

# Debug logging (very verbose)
# Set to true to log every SQL query executed by the API.
DB_LOG_SQL=false

# Security Notes:
# - JWT_SECRET should be at least 32 characters long and randomly generated
# - Use openssl rand -base64 32 to generate a secure secret
# - SMTP_PASSWORD should be an App Password for Gmail, not your regular password
# - Enable 2FA on Gmail account to generate App Passwords
# - Never commit real values to version control